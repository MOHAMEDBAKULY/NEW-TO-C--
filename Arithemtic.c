#include <stdio.h>
int main()
{
   int x, y, results;
   x = 50;
   y = 20;


    // results =  x + y;
    // results =  x - y;
    // results =  x * y;
    // results =  x % y;
    results =  x / y;

    // printf("sum of %d and %d is %d, x, y, results");
    // printf("%d + %d = %d, x, y, results");
    // printf("x + y = %d", results);
    // printf("\n x - y = %d", results);
    // printf("\n x * y = %d", results);
    // printf("\n x % y = %d", results);
    printf("\n x / y = %d", results);



    return 0;
}